# HUMHUM 1.0.0 发布防线（7 天冲刺）

状态：进行中 · 起始 2026-08-07 · 目标 7 天内可发布 · 真机验收由 owner 负责

## 范围决策（默认，可调）

- **平台**：仅 macOS Apple Silicon。Windows/Linux 明确标注为后续。
- **推荐回复**：现状是三句正则硬编码，不是 AI 推荐。1.0 改名「快捷短语/常用回复」，
  不夸大能力，不违背「正文不发给 AI」的隐私承诺。真正的 AI 推荐留 1.1。
- **远程审批语义**：远程下发的高危工具（Bash / 文件写）必须重新触发可远程批准的权限流；
  读类工具免审直跑。补齐「远程批准」卖点的安全底线。

## 两条主线

### 主线一：远程控制本地 agent —— 语义正确 + 离线不丢

- P0-1 去掉远程续写的 `--permission-mode dontAsk` 免审，高危工具走可远程批准的权限流
  （`src-tauri/src/claude_followup.rs:14` 等续写路径）。
- P0-2 权限审批离线补偿：待批请求持久化，脱离 hook HTTP 连接 120s 硬窗口
  （`src-tauri/src/commands.rs:3645` 一带）。
- P0-3 RNG 失败改 fail-closed，禁止空 nonce 流入加密封装
  （`src-tauri/src/mobile_bridge.rs:1302`）。

### 主线二：基础功能 —— 把 demo 级实现补瓷实

- P0-4 语音确认精确/前缀匹配，移除「可以/ok」泛词；高危工具禁纯语音放行，强制二次确认
  （`src/lib/voice-command/commands.ts:14`、`handler.ts`）。
- P0-5 语音识别失败/不可用给可见反馈，不再静默吞
  （`src/lib/stt/web-speech.ts:73`、`src/lib/voice-command/useVoiceCommand.ts`）。
- P0-6 Humi 聊天历史落盘 + 隐藏 Qoder/Claude 死路大脑，只留 Codex
  （`src/components/Hub/HumiModule.tsx:477`、`src-tauri/src/commands.rs:391,401`）。
- P0-7 Hush「推荐回复」改名快捷短语 + 清理 `suggested_reply` 死字段
  （`src/lib/hush/replySkill.ts:26`、`src-tauri/src/hush_store.rs:250`）。

### 发布门槛

- P0-8 清 clippy warning，把 macOS CI 与 verify skill 的 clippy 开到 `-D warnings`
  （`.github/workflows/macos-ci.yml`、`.qoder/skills/verify/SKILL.md`）。

## 明确砍到 1.1

Windows/Linux、AI 推荐回复、微信 experimental 转正、聊天流式输出、
「注入正在运行的交互式会话」（当前是 headless resume 并行跑）。

## 验收

每条 P0 需过：`npx tsc --noEmit`（前端）、`cd src-tauri && cargo fmt --check && cargo clippy && cargo test --lib`（Rust）、相关单测。
真机行为由 owner 审核。

## 进度

<!-- PROGRESS -->
- [ ] P0-1 远程续写权限语义
- [ ] P0-2 权限审批离线补偿
- [x] P0-3 RNG fail-closed
- [x] P0-4 语音确认精确匹配（含负向短语安全 bug 修复）
- [x] P0-5 语音识别可见反馈
- [x] P0-6 聊天历史落盘 + 隐藏死路大脑
- [x] P0-7 快捷短语改名 + 清死字段
- [x] P0-8 clippy 严格门禁
