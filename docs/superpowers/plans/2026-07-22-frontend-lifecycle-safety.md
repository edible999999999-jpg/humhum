# 前端生命周期与权限安全修复计划

## 任务 1：权限弹窗

- 为 `ConfirmToast` 和 `QuestionToast` 增加交互回归测试。
- 将关闭阻塞提问改为明确拒绝。
- 回答失败时保留弹窗、显示错误并允许重试。

## 任务 2：Humi 生命周期

- 增加 Hub 模块切换测试，证明 Humi 不会卸载。
- 保留 Humi 实例，仅切换其可见状态。
- 验证异步回答完成后仍写入当前聊天记录。

## 任务 3：桌宠与 Hush

- 用可测试的定时器管理方式取消旧桌宠状态回调。
- 为 Hush 状态刷新增加 single-flight 保护。
- 将状态兜底轮询和界面默认值统一为 60 秒。

## 任务 4：启动与可访问性

- 为 Tauri 运行环境判断增加测试。
- 非 Tauri 环境不调用桌面窗口 API。
- 补齐本轮发现的输入标签、按钮名称和焦点样式。

## 任务 5：验证

- 运行相关测试。
- 运行完整 `npm test -- --run`。
- 运行 `npm run build`。
- 检查 diff，确保不覆盖现有桌宠表情和其他并行改动。
